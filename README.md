First MTJ chipholder board version, I will update it with the same SMA connectors that are used on the current board. 
Four layer stack is sig/gnd/gnd/sig, though we aren't using the bottom signal layer.
Trace width and clearence were calculated with the kicad calculator for grounded coplanar waveguide with ground plane. 
